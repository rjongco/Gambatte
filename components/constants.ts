export const ROW_HEIGHT = 46; // px per lane row
export const BAR_GAP = 5; // vertical inset of a bar within its row
export const RULER_HEIGHT = 30; // px for the per-column hour ruler
export const MIN_BODY_ROWS = 6; // keep a comfortable droppable area even when empty
